% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/chromosome.R
\name{gwas_chromosome}
\alias{gwas_chromosome}
\title{Single-chromosome (regional) plot of GWAS results}
\usage{
gwas_chromosome(
  data,
  chr,
  xlim = NULL,
  threshold = 5e-08,
  suggestive = 1e-05,
  highlight = NULL,
  label = TRUE,
  label_by = c("gene", "SNP"),
  point_color = "#2f5c8f",
  highlight_color = "#d1422f",
  point_size = 1.4,
  point_alpha = 0.85,
  interactive = FALSE,
  title = NULL,
  subtitle = NULL
)
}
\arguments{
\item{data}{A GWAS data frame. It is passed through \code{\link[=validate_gwas]{validate_gwas()}}, so
raw column names (e.g. \code{BP}, \code{pvalue}) are accepted.}

\item{chr}{The chromosome to plot (matched against the \code{CHR} column, with or
without a \code{"chr"} prefix).}

\item{xlim}{Optional numeric length-2 base-pair window \code{c(start, end)} to
restrict the region shown.}

\item{threshold}{Genome-wide significance p-value drawn as a solid line
(default \code{5e-8}). Use \code{NULL} to omit.}

\item{suggestive}{Suggestive significance p-value drawn as a dashed line
(default \code{1e-5}). Use \code{NULL} to omit.}

\item{highlight}{A \code{\link[=highlight_top]{highlight_top()}} specification, a single number
(shorthand for \code{top_n}), or \code{NULL} for no highlighting. Highlighted
markers are drawn in \code{highlight_color}.}

\item{label}{Logical; label the highlighted markers (default \code{TRUE} when
\code{highlight} is given). Requires the highlighted set to be non-empty.}

\item{label_by}{Column used for labels, \code{"gene"} (default, falls back to
\code{"SNP"} if absent) or \code{"SNP"}.}

\item{point_color}{Colour for the (non-highlighted) points.}

\item{highlight_color}{Colour for highlighted markers and their labels.}

\item{point_size, point_alpha}{Size and alpha of the non-highlighted points.}

\item{interactive}{Logical; if \code{TRUE}, return an interactive \code{girafe}
widget with hover tooltips (requires \pkg{ggiraph}).}

\item{title, subtitle}{Optional plot title and subtitle.}
}
\value{
A \link[ggplot2:ggplot]{ggplot2::ggplot} object, or a \code{\link[ggiraph:girafe]{ggiraph::girafe()}} htmlwidget when
\code{interactive = TRUE}.
}
\description{
Zooms into one chromosome (optionally a base-pair window), plotting
\eqn{-\log_{10}(P)} against position in megabases. Significance lines and
top-marker highlighting work as in \code{\link[=gwas_manhattan]{gwas_manhattan()}}.
}
\examples{
data(gwas_example)
gwas_chromosome(gwas_example, chr = 1)
gwas_chromosome(gwas_example, chr = 1, highlight = highlight_top(top_n = 3))
}
\seealso{
\code{\link[=gwas_manhattan]{gwas_manhattan()}}, \code{\link[=highlight_top]{highlight_top()}}
}
