% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/highlight.R
\name{highlight_top}
\alias{highlight_top}
\title{Specify which top markers to highlight}
\usage{
highlight_top(
  top_n = NULL,
  threshold = NULL,
  snps = NULL,
  genes = NULL,
  by = NULL
)
}
\arguments{
\item{top_n}{Integer; highlight the \code{top_n} markers with the smallest
p-values (per group when \code{by} is set).}

\item{threshold}{Numeric p-value; highlight markers with \code{P <= threshold}.}

\item{snps, genes}{Character vectors of \code{SNP} ids / \code{gene} names to highlight.}

\item{by}{Optional grouping column applied before \code{top_n}
(e.g. \code{"CHR"} for the top hit on each chromosome, or \code{"trait"}).}
}
\value{
An object of class \code{"gwas_highlight"} (a list of the criteria).
}
\description{
A small constructor that bundles a highlight specification for reuse across
the plotting functions and the top-hits table. Criteria are combined as a
union: a marker is highlighted if it satisfies \emph{any} of the supplied rules.
}
\examples{
highlight_top(top_n = 10)
highlight_top(threshold = 5e-8, genes = c("GENE01000"))
highlight_top(top_n = 1, by = "CHR")  # lead SNP per chromosome
}
