% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate.R
\name{simulate_gwas}
\alias{simulate_gwas}
\title{Simulate a GWAS summary-statistics data frame}
\usage{
simulate_gwas(
  n_chr = 22,
  snps_per_chr = 1000,
  n_peaks = 8,
  traits = NULL,
  seed = NULL
)
}
\arguments{
\item{n_chr}{Number of chromosomes.}

\item{snps_per_chr}{Approximate number of SNPs per chromosome. Chromosome
lengths (and hence SNP counts) taper for higher-numbered chromosomes.}

\item{n_peaks}{Number of association peaks to inject across the genome.}

\item{traits}{Optional character vector of trait names. When supplied
(length > 1), the returned frame gains a \code{trait} column with independent
peaks per trait --- suitable for multi-ring circular plots.}

\item{seed}{Optional integer seed for reproducibility.}
}
\value{
A \link[tibble:tibble]{tibble} with columns \code{SNP}, \code{CHR}, \code{POS}, \code{P},
\code{gene}, and (when \code{traits} has length > 1) \code{trait}.
}
\description{
Generates a reproducible, tidy GWAS-style data frame with a uniform
background of null p-values plus a handful of injected association peaks.
It is used to build the package's bundled example datasets and is handy for
examples, tests and quickly trying out the plotting functions.
}
\examples{
head(simulate_gwas(n_chr = 5, snps_per_chr = 200, seed = 1))
}
