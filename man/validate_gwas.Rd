% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate.R
\name{validate_gwas}
\alias{validate_gwas}
\title{Validate and standardise a GWAS data frame}
\usage{
validate_gwas(
  data,
  snp = NULL,
  chr = NULL,
  pos = NULL,
  p = NULL,
  gene = NULL,
  trait = NULL,
  na.rm = TRUE
)
}
\arguments{
\item{data}{A data frame of GWAS summary statistics.}

\item{snp, chr, pos, p}{Optional strings giving the column names to use for
each role, overriding auto-detection.}

\item{gene, trait}{Optional strings giving the column names to use for the
(optional) gene and trait roles.}

\item{na.rm}{Logical; drop rows with a missing \code{CHR}, \code{POS} or \code{P}
(default \code{TRUE}).}
}
\value{
A \link[tibble:tibble]{tibble} with columns \code{SNP}, \code{CHR} (ordered
factor), \code{POS} (numeric), \code{P} (numeric) first, followed by \code{gene} and/or
\code{trait} when present, and then any remaining columns from \code{data}.
}
\description{
Checks that a data frame contains the columns required by \pkg{gwasplot}
and returns a tidy, type-coerced \link[tibble:tibble]{tibble} with canonical
column names. Column names are auto-detected from a set of common aliases
(for example \code{BP} or \code{position} map to \code{POS}; \code{pvalue} maps to \code{P}) or can
be supplied explicitly.
}
\details{
The canonical contract is four required columns --- \code{SNP} (marker id),
\code{CHR} (chromosome), \code{POS} (base-pair position) and \code{P} (p-value) --- plus
two optional columns, \code{gene} and \code{trait}. \code{CHR} is returned as an ordered
factor in natural genomic order (see \code{\link[=order_chromosomes]{order_chromosomes()}}). Any additional
columns in \code{data} are preserved, which is convenient for interactive
tooltips.
}
\examples{
df <- data.frame(
  marker = c("rs1", "rs2", "rs3"),
  chrom  = c(1, 1, 2),
  bp     = c(100, 200, 150),
  pvalue = c(1e-8, 0.2, 3e-3)
)
validate_gwas(df)
}
