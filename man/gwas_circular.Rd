% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/circular.R
\name{gwas_circular}
\alias{gwas_circular}
\title{Circular (CMplot-style) plot of GWAS results, with multiple trait rings}
\usage{
gwas_circular(
  data,
  trait = "trait",
  threshold = 5e-08,
  highlight = NULL,
  label = TRUE,
  label_by = c("gene", "SNP"),
  colors = NULL,
  highlight_color = "#d1422f",
  ideogram = TRUE,
  point_size = 0.9,
  point_alpha = 0.85,
  shared_scale = TRUE,
  r_inner = 0.38,
  ring_gap = 0.035,
  interactive = FALSE,
  big_data = FALSE,
  raster = NULL,
  max_points = NULL,
  title = NULL,
  subtitle = NULL
)
}
\arguments{
\item{data}{A GWAS data frame (passed through \code{\link[=validate_gwas]{validate_gwas()}}). For
multiple rings it must contain a trait column, or you can bind several
single-trait frames together and set \code{trait}.}

\item{trait}{Column that splits the data into rings (default \code{"trait"} when
present, otherwise a single ring). Set \code{NULL} to force a single ring.}

\item{threshold}{Genome-wide significance p-value drawn as a ring per trait
(default \code{5e-8}; \code{NULL} to omit).}

\item{highlight}{A \code{\link[=highlight_top]{highlight_top()}} spec, a single number (\code{top_n}), or
\code{NULL}. Applied per trait.}

\item{label}{Logical; label highlighted markers (default \code{TRUE}).}

\item{label_by}{Column for labels, \code{"gene"} (default) or \code{"SNP"}.}

\item{colors}{Optional vector of ring colours (one per trait). Recycled /
truncated as needed; defaults to a built-in palette.}

\item{highlight_color}{Colour for highlighted markers and labels.}

\item{ideogram}{Logical; draw a central chromosome colour band (default
\code{TRUE}).}

\item{point_size, point_alpha}{Size and alpha of points.}

\item{shared_scale}{Logical; if \code{TRUE} (default) all rings share one
\eqn{-\log_{10}(P)} scale so ring heights are comparable. If \code{FALSE} each
ring is scaled to its own maximum.}

\item{r_inner}{Innermost radius of the ring stack (0-1); the space inside is
used for the ideogram and labels.}

\item{ring_gap}{Radial gap between adjacent rings (0-1).}

\item{interactive}{Logical; if \code{TRUE}, return an interactive \code{girafe}
widget with per-point hover tooltips (requires \pkg{ggiraph}).}

\item{big_data}{Logical; enable the large-GWAS pipeline --- rasterise the
rings for a static plot (all points kept, via \pkg{scattermore}) or thin
to \code{max_points} for an interactive one. See \code{\link[=gwas_manhattan]{gwas_manhattan()}}.}

\item{raster}{Logical or \code{NULL}; rasterise the point layer. \code{NULL} lets
\code{big_data} decide. Requires \pkg{scattermore}.}

\item{max_points}{Optional integer; thin to about this many points with
\code{\link[=thin_gwas]{thin_gwas()}} before plotting (all hits kept).}

\item{title, subtitle}{Optional title and subtitle.}
}
\value{
A \link[ggplot2:ggplot]{ggplot2::ggplot} object, or a \code{\link[ggiraph:girafe]{ggiraph::girafe()}} htmlwidget when
\code{interactive = TRUE}.
}
\description{
Draws a circular Manhattan plot: chromosomes are arranged once around the
circle and each trait is shown as its own concentric ring, from the outside
in. Genome-wide significance rings and top-marker highlighting are supported
per trait. This is the multi-ring analogue of \code{\link[=gwas_manhattan]{gwas_manhattan()}}.
}
\examples{
data(gwas_multi)
gwas_circular(gwas_multi)                                   # 3 rings
gwas_circular(gwas_multi, highlight = highlight_top(top_n = 1, by = "trait"))

data(gwas_example)
gwas_circular(gwas_example)                                 # single ring
}
\seealso{
\code{\link[=gwas_manhattan]{gwas_manhattan()}}, \code{\link[=gwas_table]{gwas_table()}}, \code{\link[=highlight_top]{highlight_top()}}
}
