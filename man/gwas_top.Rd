% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/table.R
\name{gwas_top}
\alias{gwas_top}
\title{Extract the top GWAS markers as a tidy table}
\usage{
gwas_top(data, highlight = highlight_top(top_n = 10), columns = NULL)
}
\arguments{
\item{data}{A GWAS data frame (passed through \code{\link[=validate_gwas]{validate_gwas()}}).}

\item{highlight}{A \code{\link[=highlight_top]{highlight_top()}} spec, or a single number (\code{top_n}).
Defaults to the 10 smallest-p markers.}

\item{columns}{Optional character vector of columns to keep (and order). By
default keeps \code{SNP}, \code{gene}, \code{trait}, \code{CHR}, \code{POS}, \code{P}, \code{neg_log10_p}
when present.}
}
\value{
A \link[tibble:tibble]{tibble} of the selected markers.
}
\description{
Returns the top hits selected by a \code{\link[=highlight_top]{highlight_top()}} specification, ordered
by p-value, with a \code{neg_log10_p} column added. This is the data behind
\code{\link[=gwas_table]{gwas_table()}} and uses the same selection logic as the plots' \code{highlight}
argument, so a table and a figure can be kept perfectly in sync.
}
\examples{
data(gwas_example)
gwas_top(gwas_example, highlight = highlight_top(top_n = 5))
}
\seealso{
\code{\link[=gwas_table]{gwas_table()}}, \code{\link[=highlight_top]{highlight_top()}}
}
