% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/table.R
\name{gwas_table}
\alias{gwas_table}
\title{A formatted table of the top GWAS markers}
\usage{
gwas_table(
  data,
  highlight = highlight_top(top_n = 10),
  columns = NULL,
  color = TRUE,
  title = NULL,
  subtitle = NULL
)
}
\arguments{
\item{data}{A GWAS data frame (passed through \code{\link[=validate_gwas]{validate_gwas()}}).}

\item{highlight}{A \code{\link[=highlight_top]{highlight_top()}} spec, or a single number (\code{top_n}).
Defaults to the 10 smallest-p markers.}

\item{columns}{Optional character vector of columns to keep (and order). By
default keeps \code{SNP}, \code{gene}, \code{trait}, \code{CHR}, \code{POS}, \code{P}, \code{neg_log10_p}
when present.}

\item{color}{Logical; colour the \code{neg_log10_p} column by value
(default \code{TRUE}).}

\item{title, subtitle}{Optional table title and subtitle.}
}
\value{
A \code{gt} table object (class \code{gt_tbl}, see \code{\link[gt:gt]{gt::gt()}}). Requires the
\pkg{gt} package.
}
\description{
Builds a publication-ready \link[gt:gt]{gt} table of the top hits, mirroring the
top-markers table in the \pkg{ggvolc} package. Positions are shown with
thousands separators, p-values in scientific notation, and
\eqn{-\log_{10}(P)} is optionally colour-scaled.
}
\examples{
if (requireNamespace("gt", quietly = TRUE)) {
  data(gwas_example)
  gwas_table(gwas_example, highlight = highlight_top(top_n = 8))
}
}
\seealso{
\code{\link[=gwas_top]{gwas_top()}}, \code{\link[=gwas_manhattan]{gwas_manhattan()}}, \code{\link[=gwas_circular]{gwas_circular()}}
}
